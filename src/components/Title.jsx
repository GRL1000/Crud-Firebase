import React from "react";
import { ReactDOM } from "react";

function Title() {
    return(
        <div className="title">
            <h1>CRUD - FIREBASE</h1>
        </div>
    );
}

export default Title;
